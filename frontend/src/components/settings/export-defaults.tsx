export function ExportDefaultsSettings() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-xl font-semibold mb-1">Export Defaults</h1>
        <p className="text-sm text-muted-foreground">
          Export defaults are configured per-profile. Open a profile in the Pack Detail view to
          set output sizes, formats, and sharpening options.
        </p>
      </div>
    </div>
  );
}
